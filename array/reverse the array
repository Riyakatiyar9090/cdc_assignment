class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        // code here
        int start;
        int l=arr.size();
        int end=l-1;
       for(start=0;start<=end;start++){
           swap(arr[start],arr[end]);
           end--;
       }
    }
};
